# salias

Linux-only C++20 shared-memory messaging library scaffold.

The current M0 work establishes the documented CMake layout and the L0 platform
foundation for double-mapped shared memory and futex synchronization.
